# Task: Custom Admin Login & Registration System

- `[x]` Update `src/collections/Users.ts` — add `status` and `supabaseId` fields
- `[ ]` Create `lib/actions/admin-auth.actions.ts` — server actions for admin login/register
- `[ ]` Create `app/(payload)/admin/login/layout.tsx` — root layout for custom login page
- `[ ]` Create `app/(payload)/admin/login/page.tsx` — dark-themed admin login UI
- `[ ]` Create `app/(payload)/admin/register/layout.tsx` — root layout for register page
- `[ ]` Create `app/(payload)/admin/register/page.tsx` — admin register UI
- `[ ]` Create `app/api/seed-admin/route.ts` — API route to seed the first super-admin
